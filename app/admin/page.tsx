import{getChatGPTUser,chatGPTSignInPath,chatGPTSignOutPath}from'../chatgpt-auth';import AdminDashboard from'./dashboard';
export const dynamic='force-dynamic';const ADMIN='garbajohn101@gmail.com';
export default async function Admin(){if(process.env.NODE_ENV==='development')return <AdminDashboard email={ADMIN}/>;const user=await getChatGPTUser();if(!user)return <div className="gate"><img src="/logo.png"/><p>Church administration</p><h1>Manage Bread of Life DCM</h1><a href={chatGPTSignInPath('/admin')} target="_top">Sign in with ChatGPT</a></div>;if(user.email.toLowerCase()!==ADMIN)return <div className="gate"><h1>Access not approved</h1><p>This dashboard is restricted to the church administrator.</p><a href={chatGPTSignOutPath('/admin')}>Use another account</a></div>;return <AdminDashboard email={user.email}/>}
