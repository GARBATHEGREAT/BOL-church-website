'use client';
import{FormEvent,useState}from'react';
export default function AdminLogin(){const[error,setError]=useState(''),[busy,setBusy]=useState(false);async function login(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError('');const response=await fetch('/api/admin-login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(event.currentTarget)))});setBusy(false);if(response.ok){location.href='/admin';return}setError((await response.json()).error||'Unable to sign in.')}return <main className="gate admin-gate"><form className="login-card" onSubmit={login}><img src="/assets/images/bread-of-life-logo.webp" alt="Bread of Life logo"/><p>Church administration</p><h1>Welcome back</h1><label>Email address<input name="email" type="email" autoComplete="username" required/></label><label>Password<input name="password" type="password" autoComplete="current-password" required/></label>{error&&<div className="login-error" role="alert">{error}</div>}<button disabled={busy}>{busy?'Signing in…':'Sign in securely'}</button><a href="/">← Return to website</a></form></main>}
