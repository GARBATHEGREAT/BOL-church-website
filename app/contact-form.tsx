'use client';
import {FormEvent,useState} from 'react';
export default function ContactForm(){const[note,setNote]=useState('');const[busy,setBusy]=useState(false);async function send(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setNote('');const form=e.currentTarget;const body=Object.fromEntries(new FormData(form));const res=await fetch('/api/submissions',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});setBusy(false);if(res.ok){form.reset();setNote('Thank you. Your message has been received.')}else setNote('Your message could not be sent. Please try again.')}return <form onSubmit={send}><h3>Connect with us</h3><label>Your name<input name="name" required/></label><label>Email or phone<input name="contact" required/></label><label>I would like to…<select name="type"><option value="visit">Plan a visit</option><option value="prayer">Submit a prayer request</option><option value="ministry">Join a ministry</option><option value="question">Ask a question</option></select></label><label>Message<textarea name="message" rows={4}/></label><button disabled={busy}>{busy?'Sending…':'Send message'}</button><p aria-live="polite">{note}</p></form>}
