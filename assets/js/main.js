const toggle=document.querySelector('.nav-toggle'),nav=document.querySelector('.site-header nav');toggle?.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',open)});document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));document.querySelector('#contact-form')?.addEventListener('submit',e=>{e.preventDefault();const note=e.currentTarget.querySelector('.form-note');note.textContent='Thank you! Your message has been received in this demo.';e.currentTarget.reset()});const verses=[...document.querySelectorAll('.verse')];let verseIndex=0;function showVerse(next){verses[verseIndex]?.classList.remove('active');verseIndex=(next+verses.length)%verses.length;verses[verseIndex]?.classList.add('active')}document.querySelector('.verse-arrow.next')?.addEventListener('click',()=>showVerse(verseIndex+1));document.querySelector('.verse-arrow.prev')?.addEventListener('click',()=>showVerse(verseIndex-1));setInterval(()=>showVerse(verseIndex+1),6500);const revealTargets=document.querySelectorAll('.section:not(.hero),.impact-strip');revealTargets.forEach(el=>el.classList.add('scroll-reveal'));const revealObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');revealObserver.unobserve(entry.target)}}),{threshold:.12});revealTargets.forEach(el=>revealObserver.observe(el));const counters=document.querySelectorAll('[data-count]');const counterObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(!entry.isIntersecting)return;const el=entry.target,target=Number(el.dataset.count),start=performance.now();function tick(now){const p=Math.min((now-start)/1200,1);el.textContent=Math.round(target*(1-Math.pow(1-p,3)));if(p<1)requestAnimationFrame(tick)}requestAnimationFrame(tick);counterObserver.unobserve(el)}),{threshold:.5});counters.forEach(el=>counterObserver.observe(el));const topButton=document.querySelector('.to-top'),sections=[...document.querySelectorAll('main section[id]')],navLinks=[...document.querySelectorAll('.site-header nav a[href^="#"]')];window.addEventListener('scroll',()=>{topButton?.classList.toggle('show',scrollY>500);let current='';sections.forEach(s=>{if(scrollY>=s.offsetTop-180)current=s.id});navLinks.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+current))},{passive:true});topButton?.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));
